% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{scale_vars}
\alias{scale_vars}
\title{Scale Variables}
\usage{
scale_vars(
  data,
  Vars,
  method = c("sd", "range"),
  range = c(0, 1),
  suffix = "_s",
  group_by = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{Vars}{Character vector of numeric variable names to scale.}

\item{method}{Scaling method: "sd" (divide by SD) or "range" (min-max scaling).}

\item{range}{Numeric vector of length 2 specifying target range for "range" method.
Default c(0, 1). Use c(1, 10) for 1-10 scaling.}

\item{suffix}{Character. Suffix for new column names. Default "_s".}

\item{group_by}{Character. Name of grouping variable for group-wise scaling. Optional.}
}
\value{
Data frame with scaled variables added.
}
\description{
Scales numeric variables by dividing by standard deviation or
rescaling to a specified range. Supports group-wise scaling.
}
\examples{
\dontrun{
# Scale by SD
df <- scale_vars(mtcars, Vars = c("mpg", "hp"), method = "sd")

# Min-max scaling (0-1)
df <- scale_vars(mtcars, Vars = "mpg", method = "range")

# Scale to 1-10 range
df <- scale_vars(mtcars, Vars = "mpg", method = "range", range = c(1, 10))

# Group-wise scaling
df <- scale_vars(mtcars, Vars = "mpg", method = "sd", group_by = "cyl")
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimatenets.R
\name{estimate_single_network}
\alias{estimate_single_network}
\title{Estimate a Single Network}
\usage{
estimate_single_network(
  df,
  Vars = NULL,
  layout = "circle",
  color = DEFAULT_NODE_COLOR,
  title = "Between-person network",
  default = "EBICglasso",
  var_types = NULL,
  qgraph_args = list(),
  network_args = list(),
  verbose = TRUE,
  compute_centrality = TRUE
)
}
\arguments{
\item{df}{A data frame containing the variables to be analyzed.}

\item{Vars}{A character vector specifying the names of variables to include.
If NULL (default), all columns of the data frame are used.}

\item{layout}{Character string specifying the layout algorithm for the network plot.
Options: "circle", "spring", "groups", etc. Defaults to "circle".}

\item{color}{Character string or vector specifying node colors. Defaults to "#EEEEEE".}

\item{title}{Title for the network plot. Defaults to "Between-person network".}

\item{default}{Network estimation method for bootnet::estimateNetwork.
Options: "EBICglasso" (default), "ggmModSelect", "pcor", etc.}

\item{var_types}{Character vector of variable types for MGM ("g" = Gaussian,
"c" = categorical, "p" = Poisson). If NULL (default), types are auto-detected.}

\item{qgraph_args}{List of additional arguments passed to qgraph::qgraph.}

\item{network_args}{List of additional arguments passed to bootnet::estimateNetwork.}

\item{verbose}{Logical. If TRUE, prints progress messages. Defaults to TRUE.}

\item{compute_centrality}{Logical. If TRUE, computes centrality measures. Defaults to TRUE.}
}
\value{
A list of class "network_analysis" containing:
\itemize{
  \item{\code{network_object}}: Full network object from bootnet
  \item{\code{network_matrix}}: Adjacency/weight matrix
  \item{\code{prediction}}: MGM prediction results (R2, RMSE)
  \item{\code{centrality}}: Centrality measures (if compute_centrality = TRUE)
  \item{\code{qgraph}}: The qgraph plot object
  \item{\code{var_types}}: Variable types used for MGM
}
}
\description{
Estimates a single psychological network from a data frame using regularized
partial correlations (via bootnet) and Mixed Graphical Models (via mgm).
Provides network visualization with predictability metrics (R-squared pie charts).
}
\examples{
\dontrun{
# Basic usage
result <- estimate_single_network(
  df = my_data,
  Vars = c("var1", "var2", "var3", "var4", "var5")
)

# Access network matrix
result$network_matrix

# View predictability
result$prediction

# View centrality
result$centrality

# Custom settings
result <- estimate_single_network(
  df = my_data,
  Vars = c("var1", "var2", "var3"),
  layout = "spring",
  default = "ggmModSelect",
  var_types = c("g", "g", "c"),  # Manual type specification
  verbose = FALSE
)
}

}

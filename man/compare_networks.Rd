% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimatenets.R
\name{compare_networks}
\alias{compare_networks}
\title{Compare Two Networks}
\usage{
compare_networks(results, groups, it = 1000, test = "both", verbose = TRUE)
}
\arguments{
\item{results}{Object from estimate_grouped_networks}

\item{groups}{Character vector of length 2 specifying which groups to compare}

\item{it}{Number of permutations for NCT. Defaults to 1000.}

\item{test}{Which tests to perform: "network" (structure), "global" (strength),
"both" (default).}

\item{verbose}{Print progress. Defaults to TRUE.}
}
\value{
A list containing NCT results with p-values for network structure
  and global strength differences.
}
\description{
Performs Network Comparison Test (NCT) to compare network structure
and global strength between two groups.
}
\examples{
\dontrun{
# First estimate grouped networks
grouped_results <- estimate_grouped_networks(df, Vars, GroupVar = "group")

# Then compare two groups
comparison <- compare_networks(
  grouped_results,
  groups = c("GroupA", "GroupB"),
  it = 500
)
}

}
